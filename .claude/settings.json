{
  "permissions": {
    "allow": ["Bash", "Read", "Edit", "Write", "WebFetch", "Grep", "Glob"]
  },
  "defaultMode": "bypassPermissions",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "branch=$(git -C \"$PWD\" rev-parse --abbrev-ref HEAD 2>/dev/null); if [ \"$branch\" = \"main\" ]; then echo '{\"systemMessage\": \"You are on the main branch. Create a new feature branch before starting work (e.g. git checkout -b feat/<name>).\"}'; fi"
          }
        ]
      }
    ]
  }
}
